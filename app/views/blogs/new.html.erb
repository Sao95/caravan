<h1>新規投稿</h1>
<% if @blog.errors.any? %> <!--errors:モデルにバリデーションをかけた際、発生したエラーの内容を確認できるメソッド（any?と組み合わせる→エラーが発生しているかどうかを判定）-->
  <%= @blog.errors.count %>件のエラーが発生しました
  <% @blog.errors.full_messages.each do |message| %> <!--エラー文は配列で保存されているため、eachを使用-->
    <%= message %>
  <% end %>
<% end %>

<!-- form_with部分 -->
<%= form_with model:@blog, local:true do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>カテゴリー</h4>
  <%= f.text_field :category %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit '投稿' %>

<% end %>